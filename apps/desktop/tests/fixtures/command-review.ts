import { mount } from 'svelte';
import '../../src/styles.css';
import CommandReviewFixture from './CommandReviewFixture.svelte';
mount(CommandReviewFixture, { target: document.getElementById('app')! });
