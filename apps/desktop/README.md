# gbuild desktop

This directory is reserved for the Tauri 2 desktop app planned for milestone M1. The desktop
app will remain a thin client of `gbuildd` and is intentionally not a Cargo workspace member
until its Tauri scaffold is added.
